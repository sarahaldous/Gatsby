This site includes two counters: a regular and a hooks counter. They do the exact same, but one does so with a constructor function, and the other with React hooks.

This was an exploratory project to try out this code within a Gatsby app. It was also a chance to use a CSS-in-JS library (Emotion) and a pre-made theme (Kirkham) to do the majority of the site's CSS. 